{
  "name": "mock-interview-club",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node scripts/validate-quizzes.js",
    "test": "node --test",
    "build:site": "node scripts/build-site.js",
    "publish:quizzes": "node scripts/publish-quizzes.js"
  }
}
